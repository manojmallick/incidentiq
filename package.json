{
  "name": "incidentiq",
  "version": "2.0.0",
  "description": "IncidentIQ — DORA incident precedent search + classification agent. Gemini 3 + Google Cloud Agent Builder + Elastic MCP.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "setup:elastic": "node --env-file-if-exists=.env scripts/setup-elastic.js",
    "eval": "node scripts/eval.js"
  },
  "dependencies": {
    "@elastic/mcp-server-elasticsearch": "^0.3.1",
    "@google/genai": "^0.3.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^4.19.2",
    "google-auth-library": "^10.7.0"
  }
}
